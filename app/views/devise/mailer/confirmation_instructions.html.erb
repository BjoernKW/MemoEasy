<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
  	<p>
			<%= I18n.t('emails.registration_confirmation.body_text_1', :customer => "#{@resource.first_name} #{@resource.last_name}") %>
		</p>
		<p>
			<%= I18n.t('emails.registration_confirmation.body_text_2') %>
		</p>
    <p>
      <%= link_to I18n.t('auth.confirm'), confirmation_url(@resource, :confirmation_token => @token, :protocol => 'https') %>
    </p>
    <p>
      <%= I18n.t('emails.registration_confirmation.body_text_3') %>
    </p>
    <p>
      <%= link_to confirmation_url(@resource, :confirmation_token => @token, :protocol => 'https'), confirmation_url(@resource, :confirmation_token => @token, :protocol => 'https') %>
    </p>
  </body>
</html>
