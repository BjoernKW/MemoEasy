<%= render 'layouts/sub_navigation' %>

<div class="container-fluid">
	<div class="row">
		<%= render 'form' %>
	</div>
	<div class="row">
		<%= link_to I18n.t('show'), @assignment, :class => 'btn btn-secondary' %>
		<%= link_to I18n.t('back'), assignments_path, :class => 'btn btn-secondary' %>
	</div>
</div>
