<div class="span6 well">
	<%= simple_form_for(@assignment) do |f| %>
	  <div class="form-inputs">
	  </div>

	  <div class="form-actions">
	    <%= f.button :submit, I18n.t('assignments.create'), :class => 'btn btn-success' %>
	  </div>
	<% end %>
</div>
