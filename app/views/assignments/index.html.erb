<%= render 'layouts/sub_navigation' %>

<table class="table table-striped table-hover table-bordered">
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @assignments.each do |assignment| %>
  <tr>
    <td><%= link_to I18n.t('show'), assignment %></td>
    <td><%= link_to I18n.t('edit'), edit_assignment_path(assignment) %></td>
    <td><%= link_to I18n.t('destroy'), assignment, method: :delete, data: { confirm: I18n.t('confirm') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to I18n.t("assignments.new"), new_assignment_path %>
